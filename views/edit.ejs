<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!--LINKED CSS -->
    <link type="text/css" href="/styles/stylesheet.css" rel="stylesheet" />
    <title>Bloggy | For Funsies</title>
    <link rel="icon" href="../images/Bloggy_Icon.png" >
</head>
<body>
    <!----------HEADER---------->
    <%- include("././partials/header.ejs") %> 
    <section class="edit">
        <div class="edit-title">
            <h1>Edit Post:</h1>
        </div>

        <!-- EDIT POST -->
        <form action="/edit/<%= post.postId %>?_method=PUT" method="POST" class="edit-form">
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= post.title %>" required class="edit-title-input">
            <label for="content">Content:</label>
            <textarea id="content" name="content" required><%= post.content %></textarea>
            <div class="edit-btns">
                <a href="../" class="back-btn">Go Back</a>
                <input type="submit" value="Update Post" />
            </div>
        </form>

    </section>

    <!--------FOOTER---------->
    <%- include("././partials/footer.ejs") %> 
   <script src="/app.js" ></script>
</body>
</html>